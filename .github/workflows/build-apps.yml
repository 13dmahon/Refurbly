name: Build Mobile Apps

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Make gradlew executable
        working-directory: android
        run: chmod +x gradlew

      - name: Build Android APK
        working-directory: android
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: android/app/build/outputs/apk/release/*.apk

  build-ios:
    name: Build & Upload iOS (TestFlight)
    runs-on: macos-latest
    env:
      BUNDLE_ID: com.refurbly.app
      TEAM_ID: CR4TKVT3QV

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies & build web
        run: |
          npm ci
          npm run build

      - name: Sync Capacitor iOS
        run: npx cap sync ios

      - name: Install CocoaPods
        working-directory: ios/App
        run: pod install --repo-update

      # --- SIGNING ASSETS -----------------------------------------------------
      - name: Sanity check P12 password
        env:
          IOS_CERT_P12: ${{ secrets.IOS_CERT_P12 }}
          IOS_CERT_P12_PASSWORD: ${{ secrets.IOS_CERT_P12_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          echo "$IOS_CERT_P12" | /usr/bin/base64 -D > /tmp/dist.p12
          /usr/bin/openssl pkcs12 -in /tmp/dist.p12 \
            -passin pass:"$IOS_CERT_P12_PASSWORD" \
            -nokeys -clcerts -info -noout

      - name: Install signing assets
        env:
          IOS_CERT_P12: ${{ secrets.IOS_CERT_P12 }}
          IOS_CERT_P12_PASSWORD: ${{ secrets.IOS_CERT_P12_PASSWORD }}
          IOS_PROFILE_APPSTORE: ${{ secrets.IOS_PROFILE_APPSTORE }}
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "$HOME/certs" "$HOME/profiles" \
                   "$HOME/Library/MobileDevice/Provisioning Profiles"

          echo "$IOS_CERT_P12" | /usr/bin/base64 -D > "$HOME/certs/dist.p12"
          echo "$IOS_PROFILE_APPSTORE" | /usr/bin/base64 -D > "$HOME/profiles/AppStore.mobileprovision"

          KEYCHAIN_NAME=build.keychain-db
          KEYCHAIN_PATH="$HOME/Library/Keychains/$KEYCHAIN_NAME"

          /usr/bin/security create-keychain -p "" "$KEYCHAIN_PATH"
          /usr/bin/security set-keychain-settings "$KEYCHAIN_PATH"
          /usr/bin/security unlock-keychain -p "" "$KEYCHAIN_PATH"
          /usr/bin/security list-keychains -d user -s "$KEYCHAIN_PATH" login.keychain
          /usr/bin/security default-keychain -s "$KEYCHAIN_PATH"

          /usr/bin/security import "$HOME/certs/dist.p12" \
            -k "$KEYCHAIN_PATH" -f pkcs12 -P "$IOS_CERT_P12_PASSWORD" \
            -A -t cert -T /usr/bin/codesign -T /usr/bin/security

          /usr/bin/security set-key-partition-list -S apple-tool:,apple: -s -k "" "$KEYCHAIN_PATH"

          PROFILE_PLIST=$(mktemp)
          /usr/bin/security cms -D -i "$HOME/profiles/AppStore.mobileprovision" > "$PROFILE_PLIST"
          PROFILE_UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' "$PROFILE_PLIST")
          PROFILE_NAME=$(/usr/libexec/PlistBuddy -c 'Print :Name' "$PROFILE_PLIST")
          BID_WITH_TEAM=$(/usr/libexec/PlistBuddy -c 'Print :Entitlements:application-identifier' "$PROFILE_PLIST")
          BUNDLE_ID_IN_PROFILE="${BID_WITH_TEAM#${TEAM_ID}.}"

          cp "$HOME/profiles/AppStore.mobileprovision" \
             "$HOME/Library/MobileDevice/Provisioning Profiles/$PROFILE_UUID.mobileprovision"

          {
            echo "PROFILE_UUID=$PROFILE_UUID"
            echo "PROFILE_NAME=$PROFILE_NAME"
            echo "PROFILE_BUNDLE_ID=$BUNDLE_ID_IN_PROFILE"
            echo "KEYCHAIN_PATH=$KEYCHAIN_PATH"
          } >> "$GITHUB_ENV"

      - name: Show signing build settings
        working-directory: ios/App
        run: |
          xcodebuild -showBuildSettings -workspace App.xcworkspace -scheme App | egrep 'CODE_SIGN|PROVISIONING_PROFILE|DEVELOPMENT_TEAM|PRODUCT_BUNDLE_IDENTIFIER' || true

      # --- ARCHIVE ------------------------------------------------------------
      - name: Build archive (manual signing for App target only)
        working-directory: ios/App
        shell: bash
        run: |
          echo 'EXCLUDED_ARCHS=""' >> build.xcconfig
          echo 'ARCHS=$(ARCHS_STANDARD)' >> build.xcconfig
          echo 'BUILD_LIBRARY_FOR_DISTRIBUTION=YES' >> build.xcconfig
          echo 'OTHER_SWIFT_FLAGS=$(inherited) -no-verify-emitted-module-interface' >> build.xcconfig

          xcodebuild -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath "$PWD/build/App.xcarchive" \
            -destination 'generic/platform=iOS' \
            clean archive \
            -xcconfig build.xcconfig \
            DEVELOPMENT_TEAM="$TEAM_ID" \
            PRODUCT_BUNDLE_IDENTIFIER="$BUNDLE_ID" \
            CODE_SIGN_STYLE=Manual \
            "CODE_SIGN_IDENTITY[sdk=iphoneos*]=Apple Distribution" \
            PROVISIONING_PROFILE_SPECIFIER="$PROFILE_UUID" \
            OTHER_CODE_SIGN_FLAGS="--keychain $KEYCHAIN_PATH" \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=YES \
            "CODE_SIGNING_ALLOWED[sdk=iphonesimulator*]=NO" \
            -allowProvisioningUpdates

      - name: List signing identities in build keychain
        shell: bash
        run: security find-identity -p codesigning -v "$KEYCHAIN_PATH" || true

      # --- EXPORT (Xcode signs the .ipa here) --------------------------------
      - name: Export IPA via xcodebuild
        working-directory: ios/App
        shell: bash
        run: |
          set -euo pipefail
          cat > ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key><string>app-store</string>
            <key>teamID</key><string>${TEAM_ID}</string>
            <key>signingStyle</key><string>manual</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${BUNDLE_ID}</key><string>${PROFILE_NAME}</string>
            </dict>
            <key>stripSwiftSymbols</key><true/>
            <key>uploadSymbols</key><true/>
            <key>compileBitcode</key><false/>
          </dict>
          </plist>
          EOF

          xcodebuild -exportArchive \
            -archivePath "$PWD/build/App.xcarchive" \
            -exportOptionsPlist ExportOptions.plist \
            -exportPath "$PWD/build/export" \
            OTHER_CODE_SIGN_FLAGS="--keychain $KEYCHAIN_PATH"

      # --- VERIFY SIGNATURE AFTER EXPORT -------------------------------------
      - name: Verify exported app signature
        working-directory: ios/App
        shell: bash
        run: |
          APP_PAYLOAD="$(/usr/bin/find build/export -type d -name 'Payload' -maxdepth 2)"
          APP_PATH="$APP_PAYLOAD/App.app"
          /usr/bin/codesign --verify --deep --strict "$APP_PATH"
          /usr/bin/codesign -dv --verbose=2 "$APP_PATH" || true
          /usr/bin/security cms -D -i "$APP_PATH/embedded.mobileprovision" >/dev/null

      # --- UPLOAD TO TESTFLIGHT (robust .p8 handling) -------------------------
      - name: Upload to TestFlight
        env:
          ASC_API_KEY_ID: ${{ secrets.ASC_API_KEY_ID }}
          ASC_API_ISSUER_ID: ${{ secrets.ASC_API_ISSUER_ID }}
          ASC_API_KEY_P8: ${{ secrets.ASC_API_KEY_P8 }}
        working-directory: ios/App
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "$HOME/.private_keys"
          KEY_PATH="$HOME/.private_keys/AuthKey_${ASC_API_KEY_ID}.p8"

          # Write the .p8: accept raw PEM or base64
          if printf '%s' "$ASC_API_KEY_P8" | grep -q "BEGIN PRIVATE KEY"; then
            printf '%s' "$ASC_API_KEY_P8" > "$KEY_PATH"
          else
            printf '%s' "$ASC_API_KEY_P8" | /usr/bin/base64 -D > "$KEY_PATH"
          fi

          tr -d '\r' < "$KEY_PATH" > "${KEY_PATH}.tmp" && mv "${KEY_PATH}.tmp" "$KEY_PATH"
          chmod 600 "$KEY_PATH"

          if ! head -n1 "$KEY_PATH" | grep -q "BEGIN PRIVATE KEY"; then
            echo "::error::The AuthKey at $KEY_PATH is not a valid PEM. Ensure ASC_API_KEY_P8 is the exact .p8 contents or base64 of it."
            exit 1
          fi

          set +e
          xcrun altool --upload-app \
            --type ios \
            --file build/export/App.ipa \
            --apiKey "$ASC_API_KEY_ID" \
            --apiIssuer "$ASC_API_ISSUER_ID"
          ALSTATUS=$?
          set -e

          if [ $ALSTATUS -ne 0 ]; then
            echo "altool upload failed (code $ALSTATUS). Retrying with iTMSTransporter..."
            xcrun iTMSTransporter -m upload -assetFile build/export/App.ipa \
              -apiKey "$ASC_API_KEY_ID" -apiIssuer "$ASC_API_ISSUER_ID" -verbose
          fi

      - name: Upload iOS IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: ios/App/build/export/App.ipa
